{
  "name": "@emergencias/notify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Service 4 - Notificaciones y broadcast de estado",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=cjs --external:pg-native --minify --define:__CAOS__=false --outfile=dist/index.js",
    "build:caos": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=cjs --external:pg-native --minify --define:__CAOS__=true --outfile=dist/index.js"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.700.0",
    "@emergencias/shared": "*",
    "pg": "^8.13.0"
  }
}
